# Copy to .env (or set as Colab secrets) and fill in.
# Get a Venice API key at https://venice.ai/settings/api
VENICE_API_KEY=

# OPTIONAL — only needed for the x402 notebook (07).
# A Base mainnet wallet private key with a small USDC balance ($1+ is plenty).
# Generate a fresh one — DO NOT reuse a key holding real funds.
WALLET_PRIVATE_KEY=
